```bash

git clone https://github.com/taekungkub/react-nodejs-mongodb-starterkit.git

# run
docker compose up -d

#local frontend
http://localhost:5173/

** ถ้าฝั่ง frontend รันไม่ได้ให้ restart container frontend ใหม่เนื่องจากบางครั้งมันหา npm install ไม่เจอครับ

```
